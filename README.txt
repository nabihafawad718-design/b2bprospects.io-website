B2BProspects small targeted fix

Upload ONLY js/main.js to the clean-urls branch and replace the existing js/main.js.

This does not replace or redesign the website.
It makes CTA buttons that point to /chrome-extension/ open the published Chrome Web Store listing.

The clean trailing-slash URLs are already present in the clean-urls migration (for example /features/).
The existing .html files redirect to their clean URLs.
